import { createSlice } from "@reduxjs/toolkit";

const initialState ={
    data:[] 
}
export const remainderSlice = createSlice({
    name:"remainder",
    initialState,
    reducers:{
      

    }
})
export const {} = remainderSlice.actions
export default remainderSlice.reducer